# sass-starter-kit-serg

If you want to get started. Just run the CMD and put this command (npm install), and all the npm dependencies we need will be loaded. 
And then you can start work with Sass. Goodluck. 
